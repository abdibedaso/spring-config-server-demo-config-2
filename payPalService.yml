server:
  port: 8091

spring:
  application:
    name: payPalService
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          

activemq:
  broker:
    url: tcp://localhost:61616

server:
  port: 8092

spring:
  application:
    name: bankService
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          

activemq:
  broker:
    url: tcp://172.19.160.1:61616
    
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://172.19.160.1:8761/eureka}
  instance:
    preferIpAddress: true
